"use client";

interface Props { data: Record<string, unknown>; studentName: string; }

const URGENCY_COLOR: Record<string, string> = {
  High: "#ef4444", Medium: "#f59e0b", Low: "#10b981",
};

function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <p style={{
      fontSize: 10, fontWeight: 700, letterSpacing: "0.1em",
      textTransform: "uppercase", color: "rgba(255,255,255,0.3)", marginBottom: 14,
    }}>{children}</p>
  );
}

export default function FacultyCard({ data }: Props) {
  const actions = (data.action_items as string[]) || [];
  const urgencyColor = URGENCY_COLOR[String(data.urgency)] || "#94a3b8";
  const urgencyBadge = data.urgency === "High" ? "badge-red" : data.urgency === "Medium" ? "badge-amber" : "badge-green";
  const urgencyGlow = data.urgency === "High" ? "badge-glow-red" : data.urgency === "Medium" ? "badge-glow-amber" : "badge-glow-green";

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 20 }}>

      {/* ── Email card ─────────────────────────────────────────────── */}
      <div className="glass-premium" style={{ padding: "28px 26px" }}>

        {/* Email header */}
        <div style={{
          display: "flex", flexDirection: "row", alignItems: "flex-start",
          justifyContent: "space-between", gap: 20,
          paddingBottom: 22, marginBottom: 22,
          borderBottom: "1px solid rgba(255,255,255,0.06)",
        }}>
          <div style={{ flex: 1 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}>
              <span style={{ fontSize: 28 }}>✉️</span>
              <div>
                <p style={{ fontSize: 13, fontWeight: 700, color: "rgba(255,255,255,0.85)" }}>Faculty Intervention Note</p>
                <p style={{ fontSize: 11, color: "rgba(255,255,255,0.3)", marginTop: 2 }}>
                  Generated by EduOS AI · System Audit
                </p>
              </div>
            </div>
            <div style={{
              background: "rgba(255,255,255,0.02)", border: "1px solid rgba(255,255,255,0.05)",
              borderRadius: 12, padding: "14px 16px",
              display: "flex", flexDirection: "column", gap: 8,
            }}>
              {[["To", String(data.to)], ["From", String(data.from_agent)], ["Subject", String(data.subject)]].map(([k, v]) => (
                <div key={k} style={{ display: "flex", alignItems: "center", gap: 10 }}>
                  <span style={{ fontSize: 11, fontWeight: 600, color: "rgba(255,255,255,0.3)", width: 52, textAlign: "right", flexShrink: 0 }}>{k}:</span>
                  <span style={{
                    fontSize: 12, fontWeight: k === "Subject" ? 700 : 500,
                    color: k === "Subject" ? "#60a5fa" : "rgba(255,255,255,0.75)",
                    background: "rgba(255,255,255,0.025)", padding: "3px 10px",
                    borderRadius: 6, border: "1px solid rgba(255,255,255,0.06)",
                  }}>{v}</span>
                </div>
              ))}
            </div>
          </div>
          <div style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 10, flexShrink: 0 }}>
            <span className={`badge ${urgencyBadge} ${urgencyGlow}`} style={{ fontWeight: 700, fontSize: 12, padding: "5px 14px" }}>
              <span style={{ width: 6, height: 6, borderRadius: "50%", background: urgencyColor }} />
              {String(data.urgency)} Urgency
            </span>
            <span style={{
              fontSize: 11, color: "rgba(255,255,255,0.4)", fontWeight: 500,
              background: "rgba(255,255,255,0.03)", padding: "5px 12px",
              borderRadius: 8, border: "1px solid rgba(255,255,255,0.06)",
            }}>
              📅 Follow-up: <strong style={{ color: "rgba(255,255,255,0.65)" }}>{String(data.follow_up_date)}</strong>
            </span>
          </div>
        </div>

        {/* Executive summary */}
        <div style={{
          padding: "14px 18px", borderRadius: 12, marginBottom: 20,
          background: "rgba(255,255,255,0.02)",
          borderLeft: `3px solid ${urgencyColor}`,
          border: `1px solid rgba(255,255,255,0.04)`,
        }}>
          <p style={{ fontSize: 10, fontWeight: 700, color: "rgba(255,255,255,0.35)", letterSpacing: "0.08em", textTransform: "uppercase", marginBottom: 8 }}>
            Executive Summary
          </p>
          <p style={{ fontSize: 13, color: "rgba(255,255,255,0.68)", fontStyle: "italic", lineHeight: 1.75 }}>
            &ldquo;{String(data.summary)}&rdquo;
          </p>
        </div>

        {/* Body */}
        <div style={{
          background: "rgba(255,255,255,0.015)", border: "1px solid rgba(255,255,255,0.04)",
          borderRadius: 12, padding: "18px 20px",
          fontSize: 13, color: "rgba(255,255,255,0.72)", lineHeight: 1.85, whiteSpace: "pre-line",
        }}>
          {String(data.body)}
        </div>
      </div>

      {/* ── Action Items ────────────────────────────────────────────── */}
      {actions.length > 0 && (
        <div className="glass-premium" style={{ padding: "24px 24px" }}>
          <SectionLabel>⚡ Required Intervention Steps</SectionLabel>
          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            {actions.map((a, i) => (
              <div key={i} style={{
                display: "flex", alignItems: "flex-start", gap: 14,
                padding: "14px 16px", borderRadius: 12,
                background: "rgba(255,255,255,0.025)", border: "1px solid rgba(255,255,255,0.05)",
                transition: "background 0.15s",
              }}>
                <div style={{
                  width: 26, height: 26, borderRadius: 8, flexShrink: 0,
                  display: "flex", alignItems: "center", justifyContent: "center",
                  fontSize: 11, fontWeight: 800, color: "#10b981",
                  background: "rgba(16,185,129,0.1)", border: "1px solid rgba(16,185,129,0.2)",
                  marginTop: 1,
                }}>{i + 1}</div>
                <span style={{ fontSize: 13, color: "rgba(255,255,255,0.8)", lineHeight: 1.7, fontWeight: 500 }}>{a}</span>
              </div>
            ))}
          </div>
          <div style={{
            marginTop: 18, padding: "10px 14px", borderRadius: 10,
            background: "rgba(245,158,11,0.04)", border: "1px solid rgba(245,158,11,0.15)",
            display: "flex", alignItems: "center", gap: 10,
          }}>
            <span style={{ width: 8, height: 8, borderRadius: "50%", background: "#fbbf24", flexShrink: 0 }} />
            <p style={{ fontSize: 11.5, color: "rgba(251,191,36,0.85)", fontWeight: 600 }}>
              Human approval is required prior to taking any scheduled LMS steps.
            </p>
          </div>
        </div>
      )}
    </div>
  );
}
