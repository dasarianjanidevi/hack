"use client";

interface Props { data: Record<string, unknown>; studentName: string; }

const URGENCY_COLOR: Record<string, string> = {
  High: "#ef4444", Medium: "#f59e0b", Low: "#10b981",
};

export default function FacultyCard({ data }: Props) {
  const actions = (data.action_items as string[]) || [];
  const urgencyColor = URGENCY_COLOR[String(data.urgency)] || "#94a3b8";

  return (
    <div className="space-y-4">
      {/* Email Header */}
      <div className="glass p-5">
        <div className="flex items-start justify-between mb-4">
          <div className="flex-1">
            <div className="flex items-center gap-2 mb-3">
              <span className="text-2xl">✉️</span>
              <div>
                <p className="text-xs text-white/35">Faculty Intervention Note</p>
                <p className="text-xs font-semibold text-white/50">Generated by EduOS AI · Human review required</p>
              </div>
            </div>
            <div className="space-y-1.5 text-sm">
              <div className="flex gap-2">
                <span className="text-white/35 w-14 flex-shrink-0">To:</span>
                <span className="text-white/70">{String(data.to)}</span>
              </div>
              <div className="flex gap-2">
                <span className="text-white/35 w-14 flex-shrink-0">From:</span>
                <span className="text-white/70">{String(data.from_agent)}</span>
              </div>
              <div className="flex gap-2">
                <span className="text-white/35 w-14 flex-shrink-0">Re:</span>
                <span className="text-white/80 font-medium">{String(data.subject)}</span>
              </div>
            </div>
          </div>
          <div className="flex flex-col items-end gap-2">
            <span className="text-xs px-2.5 py-1 rounded-full font-semibold"
              style={{ background: `${urgencyColor}18`, color: urgencyColor, border: `1px solid ${urgencyColor}35` }}>
              {String(data.urgency)} Urgency
            </span>
            <span className="text-xs text-white/30">Follow-up: {String(data.follow_up_date)}</span>
          </div>
        </div>

        {/* Summary */}
        <div className="px-3 py-2.5 rounded-lg mb-4"
          style={{ background: "rgba(255,255,255,0.04)", borderLeft: `3px solid ${urgencyColor}` }}>
          <p className="text-sm text-white/60 italic">{String(data.summary)}</p>
        </div>

        {/* Body */}
        <div className="text-sm text-white/65 leading-relaxed whitespace-pre-line border-t border-white/6 pt-4">
          {String(data.body)}
        </div>
      </div>

      {/* Action Items */}
      {actions.length > 0 && (
        <div className="glass p-4">
          <p className="text-xs font-semibold text-white/40 uppercase tracking-wider mb-3">Required Actions for Faculty</p>
          <ul className="space-y-2">
            {actions.map((a, i) => (
              <li key={i} className="flex items-start gap-2.5">
                <div className="w-5 h-5 rounded flex items-center justify-center text-xs font-bold flex-shrink-0 mt-0.5"
                  style={{ background: "rgba(16,185,129,0.15)", color: "#10b981", border: "1px solid rgba(16,185,129,0.3)" }}>
                  {i + 1}
                </div>
                <span className="text-sm text-white/65">{a}</span>
              </li>
            ))}
          </ul>
          <div className="mt-4 pt-3 border-t border-white/6 flex items-center gap-2">
            <div className="w-2 h-2 rounded-full bg-amber-400" />
            <p className="text-xs text-amber-400/70">Human approval required before any action is taken</p>
          </div>
        </div>
      )}
    </div>
  );
}
