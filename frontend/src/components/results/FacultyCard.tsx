"use client";

interface Props { data: Record<string, unknown>; studentName: string; }

const URGENCY_COLOR: Record<string, string> = {
  High: "#ef4444", Medium: "#f59e0b", Low: "#10b981",
};

export default function FacultyCard({ data }: Props) {
  const actions = (data.action_items as string[]) || [];
  const urgencyColor = URGENCY_COLOR[String(data.urgency)] || "#94a3b8";
  const urgencyBadge = data.urgency === "High" ? "badge-red" : data.urgency === "Medium" ? "badge-amber" : "badge-green";
  const urgencyGlow = data.urgency === "High" ? "badge-glow-red" : data.urgency === "Medium" ? "badge-glow-amber" : "badge-glow-green";

  return (
    <div className="space-y-6">
      {/* Email Header */}
      <div className="glass-premium p-6">
        <div className="flex flex-col md:flex-row items-start justify-between gap-4 mb-6 border-b border-white/[0.05] pb-5">
          <div className="flex-1">
            <div className="flex items-center gap-3 mb-4">
              <span className="text-3xl filter drop-shadow-[0_0_6px_rgba(255,255,255,0.15)]">✉️</span>
              <div>
                <p className="text-xs font-bold text-white/40 uppercase tracking-widest">Faculty Intervention Note</p>
                <p className="text-[10px] text-white/30 font-medium">Generated by EduOS AI · System Audit Verification</p>
              </div>
            </div>
            <div className="space-y-2 text-sm bg-white/[0.01] border border-white/[0.03] p-4 rounded-xl">
              <div className="flex items-center gap-2">
                <span className="text-white/35 w-14 font-semibold text-xs text-right">To:</span>
                <span className="text-white/80 font-medium text-xs bg-white/[0.03] px-2 py-0.5 rounded border border-white/[0.05]">{String(data.to)}</span>
              </div>
              <div className="flex items-center gap-2">
                <span className="text-white/35 w-14 font-semibold text-xs text-right">From:</span>
                <span className="text-white/80 font-medium text-xs bg-white/[0.03] px-2 py-0.5 rounded border border-white/[0.05]">{String(data.from_agent)}</span>
              </div>
              <div className="flex items-center gap-2">
                <span className="text-white/35 w-14 font-semibold text-xs text-right">Subject:</span>
                <span className="text-blue-400 font-bold text-xs">{String(data.subject)}</span>
              </div>
            </div>
          </div>
          <div className="flex flex-row md:flex-col items-end gap-3 self-stretch md:self-auto justify-between md:justify-start">
            <span className={`badge ${urgencyBadge} ${urgencyGlow} font-bold text-xs px-3 py-1`}>
              <span className="w-1.5 h-1.5 rounded-full" style={{ background: urgencyColor }} />
              {String(data.urgency)} Urgency
            </span>
            <span className="text-xs text-white/40 bg-white/[0.03] border border-white/[0.05] px-2.5 py-1 rounded-lg">
              📅 Follow-up: <span className="font-semibold text-white/70">{String(data.follow_up_date)}</span>
            </span>
          </div>
        </div>

        {/* Summary */}
        <div className="px-4 py-3.5 rounded-xl mb-5"
          style={{ background: "rgba(255,255,255,0.02)", borderLeft: `4px solid ${urgencyColor}`, borderRight: "1px solid rgba(255,255,255,0.03)", borderTop: "1px solid rgba(255,255,255,0.03)", borderBottom: "1px solid rgba(255,255,255,0.03)" }}>
          <p className="text-xs font-bold text-white/40 uppercase tracking-widest mb-1">Executive Summary</p>
          <p className="text-sm text-white/70 italic leading-relaxed">"{String(data.summary)}"</p>
        </div>

        {/* Body */}
        <div className="text-sm text-white/75 leading-relaxed whitespace-pre-line bg-white/[0.01] border border-white/[0.03] rounded-xl p-5">
          {String(data.body)}
        </div>
      </div>

      {/* Action Items */}
      {actions.length > 0 && (
        <div className="glass-premium p-6">
          <p className="text-[10px] font-bold text-white/40 uppercase tracking-widest mb-4">Required Intervention Steps</p>
          <div className="space-y-3">
            {actions.map((a, i) => (
              <div key={i} className="flex items-start gap-3.5 p-3.5 rounded-xl bg-white/[0.02] border border-white/[0.04] hover:bg-white/[0.04] transition-all">
                <div className="w-6 h-6 rounded-lg flex items-center justify-center text-xs font-extrabold flex-shrink-0 mt-0.5 shadow"
                  style={{ background: "rgba(16,185,129,0.1)", color: "#10b981", border: "1px solid rgba(16,185,129,0.2)" }}>
                  {i + 1}
                </div>
                <span className="text-sm text-white/80 leading-relaxed font-medium">{a}</span>
              </div>
            ))}
          </div>
          <div className="mt-5 pt-4 border-t border-white/[0.05] flex items-center gap-3 p-3 rounded-xl bg-amber-500/[0.02] border border-amber-500/10">
            <div className="w-2 h-2 rounded-full bg-amber-400 badge-glow-amber animate-pulse" />
            <p className="text-xs text-amber-400/80 font-semibold tracking-wide">Human approval is required prior to taking any scheduled LMS steps.</p>
          </div>
        </div>
      )}
    </div>
  );
}

